# 🌐 MicroDuck RL Lab - 클라우드 무료 배포 가이드 (5분 완성)

이 가이드는 현재 로컬 컴퓨터에서 작동 중인 **MicroDuck RL Lab 웹 대시보드**를 전 세계 누구나 웹 브라우저로 접속할 수 있도록 **100% 무료(Free)**로 클라우드에 배포하는 방법을 안내합니다.

---

## 🏆 추천 배포 방식 비교

| 플랫폼 | 추천도 | 비용 | 특징 및 적합한 용도 |
| :--- | :---: | :---: | :--- |
| **1. Streamlit Community Cloud** | ⭐⭐⭐⭐⭐ (최고 추천) | **완전 무료** | GitHub 저장소 연결만으로 **원클릭 배포**. 자동 SSL(HTTPS), 나만의 서브도메인(`*.streamlit.app`) 제공 |
| **2. Hugging Face Spaces** | ⭐⭐⭐⭐ | **완전 무료** | 전 세계 AI/머신러닝 개발자 커뮤니티 노출에 최적. 16GB 무료 CPU 인스턴스 제공 |
| **3. Render / Cloud Run (Docker)** | ⭐⭐⭐ | 무료~소액 | 제공된 `Dockerfile`을 이용해 독립 컨테이너 환경으로 기업용 프로덕션 운영 |

---

## 🚀 가장 추천하는 방법: Streamlit Community Cloud (원클릭 무료 배포)

### 1단계: GitHub 저장소(Repository)에 코드 올리기

1. **GitHub([github.com](https://github.com))에 로그인**한 후 새 저장소(New repository)를 만듭니다 (예: `microduck_rl`).
2. VS Code 또는 터미널에서 다음 명령어를 실행하여 코드를 푸시합니다:

```powershell
# 1. git 초기화 (이미 되어 있다면 생략)
git init

# 2. 모든 파일 추가 (모델 파일과 비디오가 포함되도록 .gitignore가 이미 세팅되어 있습니다)
git add .

# 3. 커밋
git commit -m "feat: complete MicroDuck RL Lab web dashboard"

# 4. GitHub 저장소와 연결 (본인의 GitHub 주소 입력)
git branch -M main
git remote add origin https://github.com/본인아이디/microduck_rl.git

# 5. 푸시
git push -u origin main
```

---

### 2단계: Streamlit Community Cloud에서 배포하기

1. **[share.streamlit.io](https://share.streamlit.io/)** 에 접속하여 **"Continue with GitHub"**를 클릭해 로그인합니다.
2. 우측 상단의 **"Create app"** (또는 "New app") 버튼을 누릅니다.
3. 설정 창에서 다음 3가지만 확인합니다:
   * **Repository**: 방금 올린 저장소 선택 (예: `본인아이디/microduck_rl`)
   * **Branch**: `main`
   * **Main file path**: `app.py`
   * *(선택 사항)* **App URL**: 원하는 도메인 입력 (예: `microduck-rl-lab.streamlit.app`)
4. **"Deploy!"** 버튼을 클릭합니다! 🎉

---

### 3단계: 배포 완료 및 확인

* 1~2분 정도 빌드가 진행된 후, 전 세계 어디서든 스마트폰이나 다른 컴퓨터 브라우저로 접속할 수 있는 고유 주소가 생성됩니다:
  👉 **`https://microduck-rl-lab.streamlit.app`**
* 이후 로컬에서 코드를 수정하고 `git push`만 하면 클라우드 웹사이트가 자동으로 업데이트(CI/CD)됩니다!

---

## 🛠️ 이미 준비 완료된 핵심 호환성 설정들

Streamlit Cloud는 리눅스(Debian) 서버에서 실행되므로, MuJoCo 물리 엔진과 원활히 호환되도록 다음 설정들을 이미 프로젝트에 완벽하게 준비해 두었습니다:

1. **`packages.txt` 완비**:
   * 리눅스 서버에서 MuJoCo 오프스크린 렌더링에 필요한 OpenGL 라이브러리(`libgl1-mesa-glx`, `libglib2.0-0` 등)가 자동 설치됩니다.
2. **`.gitignore` 최적화**:
   * 클라우드 서버에서 로봇 비교 영상(`videos/*.gif`)과 학습된 모델(`models/*.zip`, `models/*.pkl`), 종합 차트(`logs/learning_curves.png`)가 누락되지 않도록 배포 파일 추적이 설정되었습니다.
3. **`Dockerfile` 완비**:
   * 컨테이너 기반 클라우드(Render, Fly.io, AWS, GCP) 배포가 필요할 때를 대비한 도커 명세서가 루트 디렉토리에 함께 준비되어 있습니다.

> **이제 나만의 AI 로봇 강화학습 연구실을 세상에 공유해 보세요! 🦆🌐**

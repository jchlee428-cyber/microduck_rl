# MicroDuck RL Lab - 전체 개발 및 연구 작업 기록 (Comprehensive Work Log)

- **기록 일시**: 2026-09-19
- **작업 디렉토리**: `c:\Users\vikin\OneDrive\바탕 화면\Antigravity IDE\microduck_rl`
- **실행 환경**: Windows 10/11, Python 3.10.11, Gymnasium 1.3, MuJoCo 3.13, PyTorch 2.14, Stable-Baselines3 2.9, Streamlit 1.64

---

## 1. 프로젝트 개요 및 목표

**MicroDuck RL Lab**은 고전 제어(CartPole)부터 시작하여 다관절 로봇 연속 제어(Hopper, Walker2d, Ant, Cheetah, Humanoid), 그리고 직접 3D 물리 모델링한 **자체 제작 2족 보행 오리 로봇(MicroDuck-v1)**까지 구축한 심층 강화학습(PPO) 종합 연구 및 실습 시뮬레이션 환경입니다.

---

## 2. 주요 기술적 문제 해결 (Engineering Breakthroughs)

1. **Windows 한글 경로 MuJoCo C-Runtime 파싱 버그 해결 (`mujoco_compat.py`)**:
   * **원인**: Windows 작업 경로에 한글(`바탕 화면` 등)이 포함된 경우 MuJoCo C 바인딩의 `mj_loadXML`이 파일 경로를 열지 못하고 파싱 에러 발생.
   * **해결**: Python의 UTF-8 파일 로더로 XML 문자열을 읽어 `mujoco.MjModel.from_xml_string`으로 전달하는 호환 패치 개발 및 전 모듈 자동 적용.
2. **Gymnasium WindowViewer 마우스 회전 파라미터 불일치 패치**:
   * **원인**: Gymnasium 최신 버전의 `WindowViewer`가 구버전 C 바인딩 인자(6개)로 `mjv_moveCamera`를 호출하여 마우스 회전 시 크래시 발생.
   * **해결**: `mujoco_compat.py`에서 인자 개수를 동적으로 변환(6개 -> 5개)하는 래퍼를 씌워 3D 뷰어 마우스 회전/줌이 매끄럽게 작동하도록 개선.
3. **가상환경 자동 부트스트랩 (`_bootstrap.py`)**:
   * 가상환경 활성화(`.venv\Scripts\Activate.ps1`)를 누락한 상태로 파이썬을 실행해도 자동으로 `.venv` 인터프리터로 리디렉션하여 의존성 충돌 방지.
4. **웹 대시보드 0.5초 즉각 로딩 (Lazy Import 최적화)**:
   * Streamlit 첫 로드 시 PyTorch, SB3 동시 로드로 인한 18초 블로킹 문제를 지연 로딩 기법으로 해결하여 0.5초 즉각 렌더링 실현.

---

## 3. 학습 완료된 7대 로봇 벤치마크 및 성적표

| 로봇 | 환경 ID | 관측/행동 차원 | 학습 전 (Random) | 학습 후 (PPO) | 성능 향상도 | 주요 달성 동작 |
| :--- | :--- | :---: | :---: | :---: | :---: | :--- |
| **🦆 MicroDuck** | `MicroDuck-v1` | 23 / 6 (연속) | `17.17` 점 | **`47.65` 점** (최대 64.5점) | **+177.5%** | **자체 제작 2족 오리**: 물갈퀴 발을 이용한 Waddle-Walk 전진 보행 |
| **🏋️ CartPole** | `CartPole-v1` | 4 / 2 (이산) | `16.00` 점 | **`500.00` 점** | **+3,025.0%** | **최대 만점 완벽 달성** (막대 100% 수직 유지) |
| **🦿 Hopper** | `Hopper-v5` | 11 / 3 (연속) | `18.04` 점 | **`400.98` 점** | **+2,122.7%** | **단각 로봇**: 150스텝 이상 안정적인 연속 전진 도약 |
| **🏃 Walker2d** | `Walker2d-v5` | 17 / 6 (연속) | `-0.12` 점 | **`377.30` 점** | **+377.4 pts** | **2족 보행**: 190스텝 이상 넘어짐 없는 직진 보행 |
| **🐜 Ant** | `Ant-v5` | 27 / 8 (연속) | `-21.39` 점 | **`454.50` 점** | **+475.8 pts** | **4족 보행**: 대각선 보행으로 1,000스텝 완주 달성 |
| **🐆 HalfCheetah**| `HalfCheetah-v5`| 17 / 6 (연속) | `-5.20` 점 | **`240.00` 점** | **+245.2 pts** | **치타 로봇**: 폭발적인 전진 질주 모션 학습 |
| **🦾 Humanoid** | `Humanoid-v5` | 376 / 17 (연속) | `122.90` 점 | **`363.00` 점** | **+195.3%** | **전신 휴머노이드**: 17개 관절 협응 직립 균형 유지 |

---

## 4. 자체 제작 시그니처 로봇: MicroDuck-v1

* **3D MJCF 모델 (`microduck.xml`)**:
  * 노란 몸통(Torso), 주황색 오리 부리(Beak), 눈, 꼬리 깃털, 날개(Wings).
  * 좌우 엉덩이(Hip), 무릎(Knee), 발목(Ankle) 각 3개씩 총 **6개 모터 액추에이터**.
  * 안정적인 착지와 마찰력을 보장하는 **넓은 물갈퀴 발(Flat Webbed Foot)** 설계.
* **Gymnasium 커스텀 환경 (`microduck_env.py`)**:
  * `Gymnasium`에 `MicroDuck-v1` 공식 환경 ID 등록.
  * **Waddle-Walk 보상 함수**: 전진 속도 보상($+1.5 v_x$), 직립 생존 보상($+1.0$), 제어 비용 페널티($-0.005 \|\mathbf{a}\|^2$), 횡방향 이탈 방지 페널티($-0.5 |v_y|$).
* **PPO 강화학습 결과**:
  * 61,440 타임스텝 학습 (약 99.7초 소요, 초당 ~620 FPS).
  * 랜덤 베이스라인(17.17점) 대비 **+177.5% 향상된 47.65점 달성**.

---

## 5. 시각화 및 모니터링 시스템 구축

1. **7대 전체 로봇 비교 애니메이션 GIF 완비 (`videos/`)**:
   * `microduck_comparison.gif`, `cartpole_comparison.gif`, `hopper_comparison.gif`, `walker2d_comparison.gif`, `ant_comparison.gif`, `halfcheetah_comparison.gif`, `humanoid_comparison.gif`
2. **8-패널 종합 학습 곡선 대시보드 (`logs/learning_curves.png`)**:
   * MicroDuck 로봇을 포함한 7개 환경의 타임스텝별 보상 상승 곡선 및 편차 밴드, 종합 요약 카드 렌더링.
3. **인터랙티브 웹 컨트롤 대시보드 (`app.py`, `launch_dashboard.py`)**:
   * **URL**: `http://127.0.0.1:8501`
   * **Tab 1: 🎬 Visual Showcase**: 7대 로봇의 훈련 전/후 비교 GIF 및 물리 관찰 포인트 설명, 3D 창 즉시 호출 버튼.
   * **Tab 2: 📊 Performance Analytics**: 8패널 종합 차트 및 인터랙티브 학습 곡선, 정량 벤치마크 표.
   * **Tab 3: 🧪 Live Telemetry**: 실시간 인퍼런스 롤아웃 및 스텝별 보상 궤적 시각화.
   * **Tab 4: ⚡ Interactive Training**: 브라우저에서 직접 PPO 하이퍼파라미터를 설정하고 실시간으로 에이전트를 훈련시키는 인터랙티브 실시간 강화학습 랩. 훈련 전후 비교 및 가중치 다운로드 지원.
   * **Tab 5: 📈 TensorBoard & System Info**: TensorBoard 바로가기 링크 및 시스템 상태 정보.
4. **실시간 TensorBoard 서버**:
   * **URL**: `http://127.0.0.1:6006`
   * 14개 학습 런에 대한 실시간 손실 함수, 정책 그래디언트, 평균 보상 스칼라 모니터링.
5. **마스터 콘솔 런처 (`launcher.py`, `run.bat`)**:
   * 숫자 또는 키 입력(`D`, `W`, `1~9`, `0`) 하나로 3D 뷰어, 벤치마크, 대시보드를 통합 제어.

---

## 6. 전체 파일 아키텍처

```text
microduck_rl/
├── microduck.xml           # 시그니처 2족 보행 오리 로봇 MuJoCo MJCF 물리 모델
├── microduck_env.py        # MicroDuck-v1 커스텀 Gymnasium 환경 (Waddle-Walk)
├── app.py                  # Streamlit 인터랙티브 웹 대시보드 메인
├── launch_dashboard.py     # 웹 대시보드 원클릭 브라우저 런처
├── launcher.py             # 마스터 대화형 콘솔 런처
├── run.bat                 # 바탕화면 원클릭 배치 파일
├── mujoco_compat.py        # Windows 한글 경로 및 마우스 뷰어 호환 패치
├── _bootstrap.py           # 가상환경 자동 리디렉션 스크립트
├── check_environment.py    # 환경 검증 스크립트
├── train_mujoco.py         # MuJoCo 로봇 및 MicroDuck PPO 학습 스크립트
├── evaluate_mujoco.py      # MuJoCo 로봇 및 MicroDuck 정량 평가 스크립트
├── train_cartpole.py       # CartPole-v1 PPO 학습 스크립트
├── evaluate_cartpole.py    # CartPole-v1 평가 스크립트
├── plot_results.py         # 8패널 통합 학습 곡선 차트 생성기
├── visualize_agent.py      # 실시간 3D 뷰어 및 비교 애니메이션 녹화기
├── requirements.txt        # 프로젝트 의존성 목록
├── models/                 # 학습 완료된 7대 로봇 가중치 (.zip) 및 정규화 파일
├── logs/                   # TensorBoard 로그, 학습 곡선 차트, 평가 데이터
└── videos/                 # 7대 전체 로봇 비교 애니메이션 (.gif)
```
